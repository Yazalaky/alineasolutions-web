# Backlog inicial

## Alta prioridad

- Integrar backend real para `LoanRequestPage` (API de leads).
- Reemplazar enlaces legales `#` por rutas reales.
- Agregar validaciones de formulario mas robustas (mascaras, mensajes por campo).
- Revisar clases Tailwind no estandar y normalizar tokens de espaciado.

## Media prioridad

- Migrar navegacion hash a `react-router-dom`.
- Añadir analytics (click en CTA, PSE, WhatsApp, envio formulario).
- Internacionalizar textos sensibles en archivo de contenido.
- Mejorar accesibilidad (focus states, landmarks, labels avanzados).

## Baja prioridad

- Suite de tests unitarios para `utils/loanCalculator.ts`.
- Pruebas E2E basicas de flujo comercial.
- Configurar CI (build + lint + test) en GitHub Actions.
